import type { Preset } from "../types.js";
import base from "../internal/base.js";

/**
 * Public placeholder only. Functional JavaScript rules belong to Phase 1.
 */
const javascript: Preset = [...base];

export default javascript;
