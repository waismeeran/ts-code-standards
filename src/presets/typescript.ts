import type { Preset } from "../types.js";
import base from "../internal/base.js";

/**
 * Public placeholder only. Non-type-aware TypeScript rules belong to Phase 2.
 */
const typescript: Preset = [...base];

export default typescript;
